import { Observable } from '@datadog/browser-core/cjs/tools/observable';
import type { ClocksState } from '@datadog/browser-core/cjs/tools/timeUtils';
interface FetchContextBase {
    method: string;
    startClocks: ClocksState;
    input: RequestInfo;
    init?: RequestInit;
    url: string;
}
export interface FetchStartContext extends FetchContextBase {
    state: 'start';
}
export interface FetchResolveContext extends FetchContextBase {
    state: 'resolve';
    status: number;
    response?: Response;
    responseType?: string;
    isAborted: boolean;
    error?: Error;
}
export declare type FetchContext = FetchStartContext | FetchResolveContext;
export declare function initFetchObservable(): Observable<FetchContext>;
export {};
