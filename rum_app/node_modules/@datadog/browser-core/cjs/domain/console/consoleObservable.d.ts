import { Observable } from '@datadog/browser-core/cjs/tools/observable';
import { ConsoleApiName } from '@datadog/browser-core/cjs/tools/display';
export interface ConsoleLog {
    message: string;
    api: ConsoleApiName;
    stack?: string;
    handlingStack?: string;
}
export declare function initConsoleObservable(apis: ConsoleApiName[]): Observable<ConsoleLog>;
