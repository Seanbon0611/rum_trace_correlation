import type { RetryInfo } from '@datadog/browser-core/cjs/transport';
import type { InitConfiguration } from '@datadog/browser-core/cjs/domain/configuration/configuration';
export declare const ENDPOINTS: {
    readonly logs: "logs";
    readonly rum: "rum";
    readonly sessionReplay: "session-replay";
};
export declare type EndpointType = keyof typeof ENDPOINTS;
export declare type EndpointBuilder = ReturnType<typeof createEndpointBuilder>;
export declare function createEndpointBuilder(initConfiguration: InitConfiguration, endpointType: EndpointType, configurationTags: string[]): {
    build(api: 'xhr' | 'fetch' | 'beacon', retry?: RetryInfo | undefined): string;
    urlPrefix: string;
    endpointType: "logs" | "rum" | "sessionReplay";
};
