import type { InitConfiguration } from '@datadog/browser-core/cjs/domain/configuration/configuration';
import type { EndpointBuilder } from '@datadog/browser-core/cjs/domain/configuration/endpointBuilder';
export interface TransportConfiguration {
    logsEndpointBuilder: EndpointBuilder;
    rumEndpointBuilder: EndpointBuilder;
    sessionReplayEndpointBuilder: EndpointBuilder;
    isIntakeUrl: (url: string) => boolean;
    replica?: ReplicaConfiguration;
    site: string;
}
export interface ReplicaConfiguration {
    applicationId?: string;
    logsEndpointBuilder: EndpointBuilder;
    rumEndpointBuilder: EndpointBuilder;
}
export declare function computeTransportConfiguration(initConfiguration: InitConfiguration): TransportConfiguration;
